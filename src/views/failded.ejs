<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed</title>
    <style>
        /* Base styles */
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f6f6f6;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            background-color: #fff;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
            max-width: 500px;
            padding: 40px;
            text-align: center;
        }

        /* Failed title */
        .failed-title {
            color: #e74c3c;
            font-size: 40px;
            font-weight: 700;
            margin-bottom: 30px;
            text-transform: uppercase;
        }

        /* Logo */
        .logo {
            width: 120px;
            height: auto;
            margin-bottom: 20px;
            animation: bounce 1.5s infinite;
        }

        /* Error message */
        p {
            font-size: 18px;
            color: #555;
            margin-bottom: 40px;
        }

        /* Home button */
        .btn-container {
            margin-top: 20px;
        }

        .home-btn {
            display: inline-block;
            padding: 12px 30px;
            background-color: #3498db;
            color: #fff;
            text-decoration: none;
            font-weight: 600;
            border-radius: 50px;
            transition: background-color 0.3s ease, transform 0.2s ease;
            text-transform: uppercase;
            box-shadow: 0 5px 15px rgba(52, 152, 219, 0.4);
        }

        .home-btn:hover {
            background-color: #2980b9;
            transform: translateY(-3px);
        }

        /* Animation */
        @keyframes bounce {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-10px);
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .container {
                padding: 30px;
            }

            .failed-title {
                font-size: 34px;
            }

            .home-btn {
                padding: 10px 24px;
                font-size: 16px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="failed-title">Payment Failed</h1>
        <img src="https://i.ibb.co.com/HGL464p/victory-zone.png" alt="Logo" class="logo">
        <p>Oops! Something went wrong. Please contact support for further assistance.</p>
        <div class="btn-container">
            <a class="home-btn" href="http://localhost:5173">Go Home</a>
        </div>
    </div>
</body>

</html>